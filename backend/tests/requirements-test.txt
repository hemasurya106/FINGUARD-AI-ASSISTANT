# Test-only dependencies for FINGUARD backend
# Install alongside the main requirements:
#   pip install -r requirements.txt -r backend/tests/requirements-test.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27          # needed by starlette TestClient
Pillow>=10.0         # already a prod dependency; listed here for completeness
